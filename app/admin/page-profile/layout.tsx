export const metadata = {
  title: "Admin Profile | Borneo Response",
  description: "Manage administrator profile and region settings.",
};

export default function AdminProfileLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
