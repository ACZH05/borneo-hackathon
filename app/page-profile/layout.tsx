export const metadata = {
  title: "Profile | Borneo Response",
  description: "Manage your responder profile and region settings.",
};

export default function ProfileLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}